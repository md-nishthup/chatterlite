// Deprecated file: previous attempt to run an Express backend inside the frontend bundle.
// Intentionally left minimal so accidental imports do not pull server code into the client build.
// All API calls should go to /api/* serverless functions now.

export {}; // keep as a module with no side effects
